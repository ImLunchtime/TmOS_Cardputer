# ESP32-C3 小车接收端 ESP-NOW 指令集

本项目的接收端使用 ESP-NOW 单向广播接收遥控器指令。遥控器只需要广播发送，不需要配对、也不需要接收回包。

## 无线参数

- Wi-Fi 模式：`WIFI_STA`
- ESP-NOW 信道：`1`
- 广播地址：`FF:FF:FF:FF:FF:FF`

遥控器与小车必须在同一信道，否则接收不到数据。

## 数据格式

发送数据为一个短字符串（ASCII），接收端按字符串匹配指令。

- 推荐直接发送 `sizeof(cmd)` 的固定长度：8 字节
- 字符串最大 7 字符，最后一个字节用于 `\0` 结束符

接收端结构体等价如下：

```c
typedef struct {
  char cmd[8];
} ControlPacket;
```

示例：发送 `"C_FD"` 时，payload 可为 `{'C','_','F','D','\0',0,0,0}`。

## 指令列表

| 功能 | 指令字符串 |
|---|---|
| 停止/刹车 | `C_ST` |
| 前进 | `C_FD` |
| 后退 | `C_BK` |
| 左平移 | `C_LS` |
| 右平移 | `C_RS` |
| 左转 | `C_TL` |
| 右转 | `C_TR` |

## WS2812（G10）指示灯规则

接收端用板载 WS2812（GPIO10/G10）显示状态与指令：

- 启动中：紫色常亮（初始化阶段）
- 就绪/停止/无信号：熄灭
- 收到移动指令：短暂闪烁对应颜色（约 160ms），随后保持该颜色，表示当前移动状态
  - `C_FD` 绿色
  - `C_BK` 红色
  - `C_LS` 蓝色
  - `C_RS` 橙黄色
  - `C_TL` 青色
  - `C_TR` 紫红色
- 收到 `C_ST`：立即熄灭
